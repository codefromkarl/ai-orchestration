# /tp-contract

Export Taskplane Eval/CI contracts as JSON.

## Usage

`/tp-contract smoke-suite`

`/tp-contract threshold-profile`

## Behavior

Runs the local wrapper:

`scripts/tp-contract $ARGS`

Use this command when you need stable smoke suite or threshold profile metadata without importing Python internals.
