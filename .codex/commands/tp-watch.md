# /tp-watch

Show current Taskplane workflow/session state.

## Usage

`/tp-watch [args]`

## Behavior

Runs the local wrapper:

`scripts/tp-watch $ARGS`

Use this command before substantial work, after interruptions, or whenever you need the latest Taskplane phase, decision state, handoff summary, next action, or completion contract.
