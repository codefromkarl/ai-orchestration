# /tp-eval

Evaluate the current Taskplane attempt report against default thresholds.

## Usage

`/tp-eval [args]`

## Behavior

Runs the local wrapper:

`scripts/tp-eval $ARGS`

Prefer JSON output when you want a machine-readable evaluation result.
